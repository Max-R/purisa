# Purisa Backend Configuration
# Copy this file to .env and fill in your credentials
# IMPORTANT: Never commit .env to git!

# =============================================================================
# BLUESKY CREDENTIALS - REQUIRED FOR DATA COLLECTION
# =============================================================================
# 1. Go to: https://bsky.app/settings/app-passwords
# 2. Click "Add App Password"
# 3. Give it a name (e.g., "Purisa Bot Detection")
# 4. Copy the generated password (you won't see it again!)
# 5. Paste your handle and password below:

BLUESKY_HANDLE=your-handle.bsky.social
BLUESKY_PASSWORD=xxxx-xxxx-xxxx-xxxx

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# SQLite (default - good for development)
DATABASE_URL=sqlite:///./purisa.db

# PostgreSQL (for production - uncomment and configure)
# DATABASE_URL=postgresql://user:password@localhost:5432/purisa

# =============================================================================
# API CONFIGURATION
# =============================================================================
API_HOST=0.0.0.0
API_PORT=8000

# CORS Origins (comma-separated, no spaces)
CORS_ORIGINS_STR=http://localhost:3000,http://localhost:5173

# =============================================================================
# LOGGING
# =============================================================================
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# BOT DETECTION SETTINGS
# =============================================================================
# Threshold score for flagging accounts (0-22 scale, default 7.0)
BOT_DETECTION_THRESHOLD=7.0

# Number of days to consider an account "new"
NEW_ACCOUNT_DAYS=30

# Posts per hour threshold for high-frequency detection
HIGH_FREQUENCY_THRESHOLD=50

# =============================================================================
# COLLECTION SETTINGS
# =============================================================================
# How often to run collection cycle (in seconds)
# 600 = 10 minutes, 300 = 5 minutes, 1800 = 30 minutes
COLLECTION_INTERVAL=600

# Default number of posts to collect per cycle
DEFAULT_POST_LIMIT=100
